{
  "name": "worktodo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "package": "node scripts/package.mjs",
    "verify:package": "node scripts/verify-package.mjs",
    "test:unit": "node --test",
    "test:e2e:shared": "playwright test --grep-invert \"两个 Dashboard 同时编辑同一 revision 时显示冲突并提供恢复|并发冲突时冲突面板可见且焦点落在载入最新版本\"",
    "test:e2e:conflict": "playwright test --grep \"两个 Dashboard 同时编辑同一 revision 时显示冲突并提供恢复|并发冲突时冲突面板可见且焦点落在载入最新版本\" --workers=1",
    "test:e2e": "npm run test:e2e:shared && npm run test:e2e:conflict",
    "test": "npm run test:unit && npm run test:e2e",
    "preview:edge": "node scripts/preview-edge.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
