# WorkTodo V1.0 验收记录

验证日期：2026-09-17

开发环境：Windows、Node.js 24.19.0、Microsoft Edge 153.0.4234.32、Playwright 1.55.0

## 自动化门禁

- [x] `npm run test:unit`：78 项通过，0 项失败。
- [x] `npm run test:e2e`：8 项通过，0 项失败。
- [x] E2E 在隔离的 Chromium 持久化配置中加载未打包扩展。
- [x] E2E 期间未观察到 HTTP 或 HTTPS 外部请求。
- [x] 关闭并重新打开浏览器上下文后，IndexedDB 任务仍可读取。
- [x] 两个 Dashboard 同时编辑同一 revision 时显示冲突，不静默覆盖。

## 浏览器视觉与响应式检查

- [x] 今日页首屏可见完成摘要、今日任务和固定快速新增入口。
- [x] 任务行不使用重复卡片，状态、计划日期和操作层级清楚。
- [x] 完成行显示“已完成 · 完成时间”，不再重复状态文案。
- [x] 390 x 844 视口下页面无横向溢出，快速新增仍可操作。
- [x] 桌面默认视口下侧栏、任务主线和列表边界符合“清透网格”方向。

浏览器预览使用 `tests/browser/dashboard-preview.html`，仅用于开发期视觉检查，不进入扩展运行时。

## 核心用户闭环

- [x] Popup 输入标题并按 Enter 后写入收集箱。
- [x] Dashboard 能看到 Popup 新建任务，刷新后仍然存在。
- [x] 今日任务可完成，并进入按实际完成日期统计的工作记录。
- [x] 取消删除确认后任务保留，原删除按钮可再次操作。
- [x] 编辑器校验失败时保持打开，Esc 关闭并恢复触发元素焦点。
- [x] JSON 导出包含版本信息，导入先预览再合并。
- [x] 删除分类必须迁移到未分类或其他分类。

## 性能记录

一次开发机实测中，合并执行今日查询、文本搜索、日统计和 JSON 导出：

| 任务数 | 耗时 |
| --- | --- |
| 1,000 | 13.1 ms |
| 5,000 | 33.8 ms |
| 10,000 | 67.6 ms |

该数值只用于当前开发机回归对比，不作为用户设备硬性指标。

## 发布边界

- [x] manifest 只声明 `storage` 权限，未声明 host permissions。
- [x] 产品运行时没有 npm、Node.js、服务器、CDN 或第三方 API 依赖。
- [x] 测试产生的 `node_modules`、`test-results` 和 Playwright 报告不会进入扩展产物。
- [ ] 统信 UOS ARM64 实机加载和 200% 缩放尚未在当前 Windows 开发机验证，发布前需在目标镜像补做。
