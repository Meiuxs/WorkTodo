# WorkTodo V1.1 Organization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 V1.1 基础数据层之上交付标签编辑与筛选、单层子任务编辑，以及父任务带未完成子任务时的完成确认。

**Architecture:** 标签和子任务继续通过服务层写入 IndexedDB，Dashboard 只处理表单、列表和确认流程。任务编辑器负责加载和保存 `tagIds`，子任务面板只对无 `parentId` 的父任务开放；父任务完成操作统一经过 `SubtaskService.completeParent`。

**Tech Stack:** Manifest V3、原生 HTML/CSS/ES Modules、IndexedDB、Node.js `node:test`、开发期 `@playwright/test`。

**Spec:** `docs/superpowers/specs/2026-09-17-worktodo-v1.1.md`

**Depends on:** `docs/superpowers/plans/2026-09-17-worktodo-v1.1-foundation.md` 全部任务完成。

## Global Constraints

- 标签使用独立实体和 `tagIds` 关联，标签改名不修改任务中的文本。
- 子任务只允许一层；有 `parentId` 的任务不能继续创建子任务。
- 父任务存在未完成子任务时必须先确认；确认后也不得自动完成子任务。
- 标签、筛选和子任务状态不能只靠颜色表达。
- 所有核心流程可通过键盘完成，弹窗关闭后焦点返回触发元素。
- 不新增权限、不读取网页内容、不发起业务网络请求。
- 页面不得直接读写 IndexedDB，必须调用 foundation 中定义的领域和服务接口。
- 每个任务先写失败测试，再写最小实现；每个任务结束提交。

## File Structure

~~~text
src/dashboard/task-editor.js             # 标签选择、标签创建、子任务面板
src/dashboard/task-list.js               # 标签文本摘要
src/dashboard/views/all-tasks-view.js    # 标签筛选
src/dashboard/dashboard-controller.js    # 父任务完成服务路由
src/dashboard/index.js                   # 注入标签和子任务服务
src/dashboard/index.html                 # 编辑器标签与子任务结构
src/dashboard/index.css                  # 标签、子任务和窄屏布局
src/services/task-query-service.js       # 按 tagId 过滤
tests/services/task-query-service.test.js
tests/services/dashboard-task-list.test.js
tests/services/dashboard-controller.test.js
tests/e2e/organization.spec.js
~~~

---

### Task O1: 在任务编辑器中创建和选择标签

**Files:**
- Modify: `src/dashboard/index.html`
- Modify: `src/dashboard/task-editor.js`
- Modify: `src/dashboard/index.js`
- Modify: `src/dashboard/index.css`
- Test: `tests/e2e/organization.spec.js`

**Interfaces:**
- Consumes: foundation 的 `TagService.list()`、`TagService.create(name)`。
- Consumes: `createTaskEditor({ getTags, onCreateTag, ... })`。
- Produces: `createTaskEditor()` 在新增和编辑任务时读写 `tagIds: string[]`。

- [ ] **Step 1: Write the failing test**

```js
import { test, expect, openDashboard } from './fixtures.js';

test('任务编辑器可以创建、选择并保留标签', async ({ extension }) => {
  const page = await openDashboard(extension);
  await page.getByLabel('记录一个新事项').fill('整理报价');
  await page.locator('#quick-add-date').selectOption('today');
  await page.getByRole('button', { name: '添加', exact: true }).click();
  await page.getByRole('button', { name: '整理报价' }).click();

  const editor = page.locator('#task-editor');
  await editor.getByLabel('新标签').fill('客户');
  await editor.getByRole('button', { name: '创建标签' }).click();
  await editor.getByRole('checkbox', { name: '客户' }).check();
  await editor.getByRole('button', { name: '保存任务' }).click();

  await page.getByRole('button', { name: '整理报价' }).click();
  await expect(editor.getByRole('checkbox', { name: '客户' })).toBeChecked();
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx playwright test tests/e2e/organization.spec.js -g "创建、选择并保留标签"`

Expected: FAIL，编辑器中不存在“新标签”输入框。

- [ ] **Step 3: Write minimal implementation**

在 `src/dashboard/index.html` 的分类字段后增加：

```html
<fieldset class="tag-picker" data-tag-picker>
  <legend>标签</legend>
  <div class="tag-picker__options" data-tag-options></div>
  <div class="tag-picker__create">
    <label for="new-tag">新标签</label>
    <input id="new-tag" maxlength="50">
    <button type="button" data-create-tag>创建标签</button>
  </div>
  <p class="form-message" data-tag-message role="alert"></p>
</fieldset>
```

在 `src/dashboard/index.js` 传入回调：

```js
import { TagService } from '../services/tag-service.js';

const tagService = new TagService(repository, { generateId: () => crypto.randomUUID() });

const taskEditor = createTaskEditor({
  dialog: document.querySelector('#task-editor'),
  getCategories: () => taskService.listCategories(),
  getTags: () => tagService.list(),
  onCreateTag: (name) => tagService.create(name),
  onCreate: (input) => controller.createTask(input),
  onUpdate: (taskId, changes, revision) => controller.editTask(taskId, changes, revision),
  onCopy: (taskId) => controller.handleTaskAction('copy', taskId),
  onReload: (taskId) => taskService.getTask(taskId),
});
```

在 `createTaskEditor` 中增加 `getTags`、`onCreateTag` 参数，并从 `../shared/ui.js` 导入 `escapeHtml`，然后实现以下局部函数：

```js
const tagOptions = dialog.querySelector('[data-tag-options]');
const newTag = dialog.querySelector('#new-tag');
const tagMessage = dialog.querySelector('[data-tag-message]');
let tags = [];

function populateTags(items, selectedIds = []) {
  const selected = new Set(selectedIds);
  tagOptions.innerHTML = items.length === 0
    ? '<p class="muted">尚未创建标签。</p>'
    : items.map((tag) => `<label class="tag-option"><input type="checkbox" name="tagIds" value="${escapeHtml(tag.id)}" ${selected.has(tag.id) ? 'checked' : ''}> ${escapeHtml(tag.name)}</label>`).join('');
}

function selectedTagIds() {
  return [...tagOptions.querySelectorAll('input[name="tagIds"]:checked')].map((input) => input.value);
}

async function loadTags(selectedIds = []) {
  tags = await getTags();
  populateTags(tags, selectedIds);
}
```

在 `openNew` 和 `openTask` 的 `populateCategories` 后分别调用 `await loadTags(defaults.tagIds ?? [])` 和 `await loadTags(task.tagIds ?? [])`。`readChanges()` 增加 `tagIds: selectedTagIds()`。

创建标签按钮处理为：

```js
dialog.querySelector('[data-create-tag]').addEventListener('click', async () => {
  const selected = selectedTagIds();
  tagMessage.textContent = '';
  try {
    await onCreateTag(newTag.value);
    newTag.value = '';
    await loadTags(selected);
    newTag.focus();
  } catch (error) {
    tagMessage.textContent = error.message;
    newTag.focus();
  }
});
```

在 `src/dashboard/index.css` 增加稳定的标签选项布局，选中状态同时使用 `:checked` 和文字勾选符号：

```css
.tag-picker { margin: 16px 0 0; border: 0; padding: 0; }
.tag-picker legend { margin-bottom: 7px; color: var(--muted); font-size: var(--text-xs); }
.tag-picker__options { display: flex; flex-wrap: wrap; gap: 7px; min-height: 30px; }
.tag-option { display: flex; align-items: center; gap: 5px; border: 1px solid var(--line); border-radius: 6px; padding: 5px 8px; }
.tag-option:has(input:checked) { border-color: var(--teal); background: var(--teal-soft); color: var(--teal); }
.tag-option:has(input:checked)::after { content: "✓"; }
.tag-picker__create { display: flex; gap: 7px; margin-top: 8px; }
.tag-picker__create input { flex: 1; min-width: 0; }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx playwright test tests/e2e/organization.spec.js -g "创建、选择并保留标签"`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/dashboard/index.html src/dashboard/task-editor.js src/dashboard/index.js src/dashboard/index.css tests/e2e/organization.spec.js
git commit -m "feat: add tag editing to task editor"
```

### Task O2: 展示任务标签并支持标签筛选

**Files:**
- Modify: `src/dashboard/task-list.js`
- Modify: `src/dashboard/views/all-tasks-view.js`
- Modify: `src/dashboard/index.js`
- Modify: `src/services/task-query-service.js`
- Modify: `src/dashboard/index.css`
- Modify: `tests/services/task-query-service.test.js`
- Modify: `tests/services/dashboard-task-list.test.js`
- Modify: `tests/e2e/organization.spec.js`

**Interfaces:**
- Consumes: `TagService.list()` 返回 `{ id, name, createdAt, updatedAt }[]`。
- Produces: `TaskQueryService.search({ tagId })` 只返回包含该标签的任务。
- Produces: `taskTagLabel(task, tags)` 返回形如 `#客户 #报价` 的文本，没有标签时返回空字符串。

- [ ] **Step 1: Write the failing tests**

在 `tests/services/task-query-service.test.js` 增加：

```js
test('search 按 tagId 过滤且保留多标签交集规则', async () => {
  const { query } = createQuery([
    task({ id: 'both', tagIds: ['customer', 'quote'] }),
    task({ id: 'customer-only', tagIds: ['customer'] }),
    task({ id: 'none', tagIds: [] }),
  ]);

  const result = await query.search({ tagId: 'customer' });

  assert.deepEqual(result.map((item) => item.id), ['both', 'customer-only']);
});
```

在 `tests/services/dashboard-task-list.test.js` 增加：

```js
import { taskTagLabel } from '../../src/dashboard/task-list.js';

test('任务行标签使用文本表达而不是只靠颜色', () => {
  const tags = [{ id: 'customer', name: '客户' }, { id: 'quote', name: '报价' }];
  assert.equal(taskTagLabel({ tagIds: ['customer', 'missing'] }, tags), '#客户');
  assert.equal(taskTagLabel({ tagIds: [] }, tags), '');
});
```

在 `tests/e2e/organization.spec.js` 追加：

```js
test('全部任务可以按标签筛选并显示标签文字', async ({ extension }) => {
  const page = await openDashboard(extension);
  await page.getByLabel('记录一个新事项').fill('客户甲报价');
  await page.locator('#quick-add-date').selectOption('today');
  await page.getByRole('button', { name: '添加', exact: true }).click();
  await page.getByRole('button', { name: '客户甲报价' }).click();
  const editor = page.locator('#task-editor');
  await editor.getByLabel('新标签').fill('客户');
  await editor.getByRole('button', { name: '创建标签' }).click();
  await editor.getByRole('checkbox', { name: '客户' }).check();
  await editor.getByRole('button', { name: '保存任务' }).click();

  await page.getByRole('button', { name: '全部任务' }).click();
  await page.getByLabel('标签').selectOption({ label: '客户' });
  await expect(page.getByRole('button', { name: '客户甲报价' })).toBeVisible();
  await expect(page.getByText('#客户', { exact: true })).toBeVisible();
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --test tests/services/task-query-service.test.js tests/services/dashboard-task-list.test.js`

Expected: FAIL，`tagId` 不参与过滤且 `taskTagLabel` 未导出。

- [ ] **Step 3: Write minimal implementation**

在 `src/services/task-query-service.js` 的 `matchesFilters` 中加入：

```js
if (filters.tagId !== undefined && !(task.tagIds ?? []).includes(filters.tagId)) return false;
```

在 `src/dashboard/task-list.js` 导出：

```js
export function taskTagLabel(task, tags) {
  const names = new Map(tags.map((tag) => [tag.id, tag.name]));
  return (task.tagIds ?? [])
    .map((id) => names.get(id))
    .filter(Boolean)
    .map((name) => `#${name}`)
    .join(' ');
}
```

`taskMarkup(task, today, tags)` 的元信息增加：

```js
const tagText = taskTagLabel(task, tags);
const tagMarkup = tagText.length === 0
  ? ''
  : ` · <span class="task__tags">${escapeHtml(tagText)}</span>`;
```

将 `${task.starred ? ' · 已星标' : ''}${tagMarkup}` 放入任务元信息。`renderTaskList` 增加 `tags = []` 参数并传给 `taskMarkup`。

在 `src/dashboard/views/all-tasks-view.js` 中：

```js
const EMPTY_FILTERS = Object.freeze({
  text: '', lifecycle: '', fromDate: '', toDate: '', categoryId: '', tagId: '', priority: '', starred: '',
});

function tagOptions(tags, selected) {
  return [
    `<option value="" ${selected === '' ? 'selected' : ''}>全部标签</option>`,
    ...tags.map((tag) => `<option value="${escapeHtml(tag.id)}" ${selected === tag.id ? 'selected' : ''}>${escapeHtml(tag.name)}</option>`),
  ].join('');
}
```

`queryFilters` 增加 `...(filters.tagId === '' ? {} : { tagId: filters.tagId })`。在 `index.js` 的 `viewOptions` 中传入 `tagService`，`createAllTasksView` 从 `tagService.list()` 加载标签，增加“标签”选择框，并在 `renderTaskList` 参数中传入 `tags`。

在 `src/dashboard/index.css` 增加：

```css
.task__tags { color: var(--teal); font-weight: 700; }
.filters { grid-template-columns: minmax(180px, 1.4fr) repeat(4, minmax(120px, .7fr)); }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `node --test tests/services/task-query-service.test.js tests/services/dashboard-task-list.test.js`

Run: `npx playwright test tests/e2e/organization.spec.js -g "按标签筛选"`

Expected: 两条命令均 PASS。

- [ ] **Step 5: Commit**

```bash
git add src/dashboard/task-list.js src/dashboard/views/all-tasks-view.js src/dashboard/index.js src/services/task-query-service.js src/dashboard/index.css tests/services/task-query-service.test.js tests/services/dashboard-task-list.test.js tests/e2e/organization.spec.js
git commit -m "feat: filter tasks by tags"
```

### Task O3: 在任务编辑器中管理单层子任务

**Files:**
- Modify: `src/dashboard/index.html`
- Modify: `src/dashboard/task-editor.js`
- Modify: `src/dashboard/index.js`
- Modify: `src/dashboard/index.css`
- Modify: `tests/e2e/organization.spec.js`

**Interfaces:**
- Consumes: `SubtaskService.createSubtask(parentId, input)`、`SubtaskService.list(parentId)`。
- Produces: `createTaskEditor({ subtaskService, ... })`，仅对 `parentId === null` 的已保存任务显示子任务面板。

- [ ] **Step 1: Write the failing test**

```js
test('父任务编辑器可以新增并持久化子任务', async ({ extension }) => {
  const page = await openDashboard(extension);
  await page.getByLabel('记录一个新事项').fill('准备投标');
  await page.locator('#quick-add-date').selectOption('today');
  await page.getByRole('button', { name: '添加', exact: true }).click();
  await page.getByRole('button', { name: '准备投标' }).click();

  const editor = page.locator('#task-editor');
  await editor.getByLabel('添加子任务').fill('整理资质文件');
  await editor.getByRole('button', { name: '添加子任务', exact: true }).click();
  await expect(editor.getByRole('button', { name: '整理资质文件' })).toBeVisible();
  await editor.getByRole('button', { name: '保存任务' }).click();

  await page.getByRole('button', { name: '准备投标' }).click();
  await expect(editor.getByRole('button', { name: '整理资质文件' })).toBeVisible();
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx playwright test tests/e2e/organization.spec.js -g "新增并持久化子任务"`

Expected: FAIL，编辑器不存在“添加子任务”输入框。

- [ ] **Step 3: Write minimal implementation**

在任务编辑器的“更多信息”之后增加：

```html
<section class="editor-subtasks" data-editor-subtasks hidden>
  <h3>子任务</h3>
  <div class="subtask-list" data-subtask-list></div>
  <div class="subtask-create">
    <label for="subtask-title">添加子任务</label>
    <input id="subtask-title" maxlength="200">
    <button type="button" data-subtask-create>添加子任务</button>
  </div>
  <p class="form-message" data-subtask-message role="alert"></p>
</section>
```

在 `src/dashboard/index.js` 创建服务并注入：

```js
import { SubtaskService } from '../services/subtask-service.js';

const subtaskService = new SubtaskService(taskService, repository);

const taskEditor = createTaskEditor({
  dialog: document.querySelector('#task-editor'),
  getCategories: () => taskService.listCategories(),
  getTags: () => tagService.list(),
  onCreateTag: (name) => tagService.create(name),
  subtaskService,
  onCreate: (input) => controller.createTask(input),
  onUpdate: (taskId, changes, revision) => controller.editTask(taskId, changes, revision),
  onCopy: (taskId) => controller.handleTaskAction('copy', taskId),
  onReload: (taskId) => taskService.getTask(taskId),
});
```

在 `createTaskEditor` 中实现：

```js
const subtasks = dialog.querySelector('[data-editor-subtasks]');
const subtaskList = dialog.querySelector('[data-subtask-list]');
const subtaskTitle = dialog.querySelector('#subtask-title');
const subtaskMessage = dialog.querySelector('[data-subtask-message]');

async function renderSubtasks() {
  const canManage = currentTask !== null && currentTask.parentId === null;
  subtasks.hidden = !canManage;
  if (!canManage) {
    subtaskList.replaceChildren();
    subtaskTitle.value = '';
    return;
  }
  const items = await subtaskService.list(currentTask.id);
  subtaskList.innerHTML = items.length === 0
    ? '<p class="empty">还没有子任务。</p>'
    : items.map((task) => `<button type="button" class="subtask-row" data-subtask-id="${escapeHtml(task.id)}">${escapeHtml(task.title)} <span>${task.lifecycle === 'completed' ? '已完成' : '待办'}</span></button>`).join('');
}
```

在 `openNew` 中调用 `renderSubtasks()` 前清空 `currentTask`，在 `openTask` 设置 `currentTask` 后调用 `await renderSubtasks()`。点击子任务标题时通过 `onReload(taskId)` 获取任务并再次 `openTask`。

添加按钮处理为：

```js
dialog.querySelector('[data-subtask-create]').addEventListener('click', async () => {
  if (currentTask === null) return;
  subtaskMessage.textContent = '';
  try {
    await subtaskService.createSubtask(currentTask.id, { title: subtaskTitle.value });
    subtaskTitle.value = '';
    await renderSubtasks();
    subtaskTitle.focus();
  } catch (error) {
    subtaskMessage.textContent = error.message;
    subtaskTitle.focus();
  }
});
```

在 `src/dashboard/index.css` 增加：

```css
.editor-subtasks { margin-top: 18px; border-top: 1px solid var(--line); padding-top: 14px; }
.editor-subtasks h3 { margin: 0 0 8px; font-size: var(--text-md); }
.subtask-list { display: grid; border-top: 1px solid var(--line); }
.subtask-row { display: flex; justify-content: space-between; gap: 10px; border: 0; border-bottom: 1px solid var(--line); background: transparent; color: var(--ink); padding: 8px 0; text-align: left; }
.subtask-row span { color: var(--muted); font-size: var(--text-xs); }
.subtask-create { display: grid; grid-template-columns: minmax(0, 1fr) auto; gap: 7px; margin-top: 10px; }
.subtask-create label { grid-column: 1 / -1; color: var(--muted); font-size: var(--text-xs); }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx playwright test tests/e2e/organization.spec.js -g "新增并持久化子任务"`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/dashboard/index.html src/dashboard/task-editor.js src/dashboard/index.js src/dashboard/index.css tests/e2e/organization.spec.js
git commit -m "feat: manage subtasks in task editor"
```

### Task O4: 父任务完成前确认未完成子任务

**Files:**
- Modify: `src/dashboard/dashboard-controller.js`
- Modify: `src/dashboard/index.js`
- Modify: `tests/services/dashboard-controller.test.js`
- Modify: `tests/e2e/organization.spec.js`

**Interfaces:**
- Consumes: `SubtaskService.activeCount(parentId)`、`SubtaskService.completeParent(parentId, revision, { force })`。
- Produces: `DashboardController.handleTaskAction('complete', id, revision, { force })`；普通任务仍可只传四个参数调用。

- [ ] **Step 1: Write the failing tests**

在 `tests/services/dashboard-controller.test.js` 增加：

```js
test('完成父任务时通过 SubtaskService 并传递确认结果', async () => {
  const calls = [];
  const view = { async render() {} };
  const controller = new DashboardController({
    views: { today: view },
    taskService: {},
    subtaskService: {
      async completeParent(id, revision, options) {
        calls.push([id, revision, options]);
        return { task: { id, revision: revision + 1 } };
      },
    },
  });

  await controller.handleTaskAction('complete', 'parent-1', 4, { force: true });

  assert.deepEqual(calls, [['parent-1', 4, { force: true }]]);
});
```

在 `tests/e2e/organization.spec.js` 增加：

```js
test('父任务有未完成子任务时先确认且不自动完成子任务', async ({ extension }) => {
  const page = await openDashboard(extension);
  await page.getByLabel('记录一个新事项').fill('发布版本');
  await page.locator('#quick-add-date').selectOption('today');
  await page.getByRole('button', { name: '添加', exact: true }).click();
  await page.getByRole('button', { name: '发布版本' }).click();
  const editor = page.locator('#task-editor');
  await editor.getByLabel('添加子任务').fill('执行回归测试');
  await editor.getByRole('button', { name: '添加子任务', exact: true }).click();
  await editor.getByRole('button', { name: '保存任务' }).click();

  const parent = page.locator('[data-task-id]').filter({ has: page.getByRole('button', { name: '发布版本' }) });
  await parent.getByRole('checkbox', { name: '完成任务' }).click();
  const confirmation = page.locator('#confirm-dialog');
  await expect(confirmation.getByRole('heading', { name: '仍有未完成子任务' })).toBeVisible();
  await confirmation.getByRole('button', { name: '取消' }).click();
  await expect(page.getByRole('button', { name: '发布版本' })).toBeVisible();

  await parent.getByRole('checkbox', { name: '完成任务' }).click();
  await confirmation.getByRole('button', { name: '仍然完成' }).click();
  await page.getByRole('button', { name: '已完成' }).click();
  await expect(page.getByRole('button', { name: '发布版本' })).toBeVisible();
  await page.getByRole('button', { name: '发布版本' }).click();
  await expect(editor.getByRole('button', { name: '执行回归测试' })).toContainText('待办');
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --test tests/services/dashboard-controller.test.js`

Expected: FAIL，`DashboardController` 不使用 `subtaskService.completeParent`。

- [ ] **Step 3: Write minimal implementation**

在 `DashboardController` 构造函数中增加 `subtaskService = null` 并保存私有字段。`handleTaskAction` 的完成分支改为：

```js
if (action === 'complete' && this.#subtaskService !== null) {
  const force = value?.force === true;
  result = await this.#subtaskService.completeParent(taskId, revision, { force });
} else if (action === 'postpone' || action === 'reschedule') {
  result = await this.#taskService[action](taskId, value, revision);
} else if (methods[action] === 'copy') {
  result = await this.#taskService.copy(taskId);
} else if (methods[action] !== undefined) {
  result = await this.#taskService[methods[action]](taskId, revision);
} else {
  throw new Error(`不支持的任务操作: ${action}`);
}
```

在 `src/dashboard/index.js` 的 `onAction` 中，先处理父任务确认：

```js
if (action === 'complete') {
  const count = await subtaskService.activeCount(taskId);
  let force = false;
  if (count > 0) {
    force = await confirmAction({
      title: '仍有未完成子任务',
      message: `还有 ${count} 个子任务未完成。确认只会完成父任务，不会自动完成这些子任务。`,
      confirmLabel: '仍然完成',
    });
    if (!force) return null;
  }
  const result = await controller.handleTaskAction('complete', taskId, revision, { force });
  showToast('已完成任务', {
    actionLabel: '撤销',
    onAction: () => controller.handleTaskAction('restore', taskId, result.task.revision).catch(onError),
  });
  return result;
}
```

将 `subtaskService` 传给 `DashboardController`：

```js
controller = new DashboardController({
  views,
  taskService,
  subtaskService,
  sendMessage: (message) => chrome.runtime.sendMessage(message),
});
```

删除 `onAction` 中原来的 `action === 'complete'` 分支，避免完成操作执行两次。

- [ ] **Step 4: Run tests to verify they pass**

Run: `node --test tests/services/dashboard-controller.test.js`

Run: `npx playwright test tests/e2e/organization.spec.js -g "不自动完成子任务"`

Expected: 两条命令均 PASS。

- [ ] **Step 5: Commit**

```bash
git add src/dashboard/dashboard-controller.js src/dashboard/index.js tests/services/dashboard-controller.test.js tests/e2e/organization.spec.js
git commit -m "feat: confirm parent completion with active subtasks"
```

### Task O5: 覆盖组织能力的键盘、焦点和窄屏回归

**Files:**
- Modify: `src/dashboard/index.css`
- Modify: `tests/e2e/organization.spec.js`

**Interfaces:**
- Consumes: O1-O4 的完整标签与子任务交互。
- Produces: 390px 视口下的标签筛选、编辑器子任务面板和焦点回归测试。

- [ ] **Step 1: Write the failing test**

```js
test('标签和子任务在键盘与 390px 视口下不溢出并恢复焦点', async ({ extension }) => {
  const page = await openDashboard(extension);
  await page.setViewportSize({ width: 390, height: 844 });
  await page.getByLabel('记录一个新事项').fill('窄屏组织任务');
  await page.locator('#quick-add-date').selectOption('today');
  await page.getByRole('button', { name: '添加', exact: true }).click();
  await page.getByRole('button', { name: '窄屏组织任务' }).focus();
  await page.keyboard.press('Enter');

  const editor = page.locator('#task-editor');
  await expect(editor.getByLabel('任务名称')).toBeFocused();
  await editor.getByLabel('新标签').fill('行政');
  await page.keyboard.press('Tab');
  await page.keyboard.press('Enter');
  await editor.getByLabel('添加子任务').fill('检查附件');
  await editor.getByRole('button', { name: '添加子任务', exact: true }).click();
  await page.keyboard.press('Escape');

  await expect(page.getByRole('button', { name: '窄屏组织任务' })).toBeFocused();
  const overflow = await page.evaluate(() => ({
    scrollWidth: document.documentElement.scrollWidth,
    clientWidth: document.documentElement.clientWidth,
  }));
  expect(overflow.scrollWidth).toBeLessThanOrEqual(overflow.clientWidth + 1);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx playwright test tests/e2e/organization.spec.js -g "不溢出并恢复焦点"`

Expected: FAIL，标签创建按钮或子任务面板在窄屏下产生横向溢出。

- [ ] **Step 3: Write minimal implementation**

在 `src/dashboard/index.css` 的 `@media (max-width: 680px)` 中加入：

```css
.tag-picker__create { grid-template-columns: 1fr; display: grid; }
.tag-picker__options { align-items: stretch; }
.tag-option { flex: 1 1 calc(50% - 7px); min-width: 0; }
.subtask-create { grid-template-columns: 1fr; }
.subtask-create button { width: 100%; }
```

确认 `createTaskEditor.close()` 仍优先把焦点还给 `previous`，当触发元素被重绘时回退到相同任务的“编辑”按钮。不得在子任务创建后调用全局 `focus()` 抢走编辑器的焦点。

- [ ] **Step 4: Run test to verify it passes**

Run: `npx playwright test tests/e2e/organization.spec.js`

Expected: PASS，组织能力全部 E2E 用例通过。

- [ ] **Step 5: Commit**

```bash
git add src/dashboard/index.css tests/e2e/organization.spec.js
git commit -m "test: cover organization accessibility and responsive layout"
```

## Self-Review

### Spec coverage

- 6.6 标签独立实体与任务关联：O1、O2。
- 7.4 筛选可见、可清除：O2。
- 6.6 单层子任务和父任务完成确认：O3、O4。
- 12 键盘、焦点、非颜色化状态：O2、O5。
- 13 390px 窄屏与长内容：O5。

### Placeholder scan

- 无 TBD、TODO 或“按需补充”。
- 所有接口名称、DOM 选择器和测试命令均已固定。

### Type consistency

- 标签统一使用 `tagIds: string[]` 和 `tagId` 筛选参数。
- 子任务统一使用 `createSubtask`、`list`、`activeCount`、`completeParent`。
- 父任务完成确认统一传递 `{ force: boolean }`。
