# WorkTodo V1.1 Planning Views Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付明天、本周和月历视图，并让周/月统计总结只依据本地任务数据生成可复制文本。

**Architecture:** 先把本地日期区间计算集中到 `domain/dates.js`，再由 `TaskQueryService` 提供范围和分组查询。周视图按七天分段，月历最多渲染六周网格；统计总结复用现有 `StatisticsService` 的指标口径，不读取网络、不调用 AI。

**Tech Stack:** Manifest V3、原生 HTML/CSS/ES Modules、IndexedDB、Node.js `node:test`、开发期 `@playwright/test`。

**Spec:** `docs/superpowers/specs/2026-09-17-worktodo-v1.1.md`

**Depends on:** `docs/superpowers/plans/2026-09-17-worktodo-v1.1-foundation.md` 全部任务完成。

## Global Constraints

- 日期使用浏览器系统本地时区，格式固定为 `YYYY-MM-DD`。
- 日期范围边界包含起止日期，不能使用 UTC 字符串切分代替本地日期。
- 工作记录按 `completedAt` 的本地日期计算，计划视图按 `scheduledDate` 计算。
- 周视图和月历必须支持键盘打开任务，状态使用文字而非只靠颜色。
- 不新增权限、不读取网页、历史、密码、Cookie 或剪贴板。
- 复制总结只能在用户点击按钮后写入剪贴板，禁止读取剪贴板。
- 页面不得直接读写 IndexedDB，必须调用服务层。
- 每个任务先写失败测试，再写最小实现；每个任务结束提交。

## File Structure

~~~text
src/domain/dates.js                    # 本地日期范围、周起点、月边界
src/services/task-query-service.js    # range、tomorrow、week、month
src/services/statistics-service.js    # 月统计与统一日期工具
src/services/summary-service.js       # 周/月文本总结
src/dashboard/views/week-view.js      # 本周七段视图
src/dashboard/views/month-view.js     # 月历网格
src/dashboard/views/history-view.js   # 总结生成与复制
src/dashboard/index.html              # 导航入口
src/dashboard/index.js                # 路由、视图和服务装配
src/dashboard/index.css               # 周/月布局与响应式
tests/domain/dates.test.js
tests/services/task-query-service.test.js
tests/services/statistics-service.test.js
tests/services/summary-service.test.js
tests/services/performance-scenarios.test.js
tests/e2e/planning.spec.js
~~~

---

### Task P1: 提供本地日期范围工具和任务范围查询

**Files:**
- Modify: `src/domain/dates.js`
- Modify: `src/services/task-query-service.js`
- Modify: `tests/domain/dates.test.js`
- Modify: `tests/services/task-query-service.test.js`

**Interfaces:**
- Produces: `addLocalDays(date, days)`、`startOfWeek(date, weekStartsOn = 1)`、`endOfWeek(date, weekStartsOn = 1)`、`startOfMonth(date)`、`endOfMonth(date)`、`eachLocalDate(startDate, endDate)`。
- Produces: `TaskQueryService.range(fromDate, toDate, filters = {}) -> Task[]`，返回未删除且 `scheduledDate` 在闭区间内的任务，按日期、开始时间、优先级和创建时间排序。

- [ ] **Step 1: Write the failing tests**

在 `tests/domain/dates.test.js` 增加：

```js
test('本地日期范围工具跨月跨年且周一作为默认周起点', () => {
  assert.equal(addLocalDays('2026-09-30', 1), '2026-10-01');
  assert.equal(addLocalDays('2026-12-31', 1), '2027-01-01');
  assert.equal(startOfWeek('2026-09-17'), '2026-09-14');
  assert.equal(endOfWeek('2026-09-17'), '2026-09-20');
  assert.equal(startOfMonth('2026-09-17'), '2026-09-01');
  assert.equal(endOfMonth('2026-09-17'), '2026-09-30');
  assert.deepEqual(eachLocalDate('2026-09-28', '2026-10-02'), [
    '2026-09-28', '2026-09-29', '2026-09-30', '2026-10-01', '2026-10-02',
  ]);
});
```

在 `tests/services/task-query-service.test.js` 增加：

```js
test('range 返回闭区间内未删除任务并按计划顺序排序', async () => {
  const { query } = createQuery([
    task({ id: 'late', scheduledDate: '2026-09-18', startTime: '15:00' }),
    task({ id: 'early', scheduledDate: '2026-09-17', startTime: '09:00' }),
    task({ id: 'outside', scheduledDate: '2026-09-20' }),
    task({ id: 'trashed', scheduledDate: '2026-09-17', trashedAt: NOW }),
  ]);

  const result = await query.range('2026-09-17', '2026-09-18');

  assert.deepEqual(result.map((item) => item.id), ['early', 'late']);
  await assert.rejects(query.range('2026-09-18', '2026-09-17'), ValidationError);
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --test tests/domain/dates.test.js tests/services/task-query-service.test.js`

Expected: FAIL，日期工具和 `range()` 未定义。

- [ ] **Step 3: Write minimal implementation**

在 `src/domain/dates.js` 增加：

```js
export function addLocalDays(date, days) {
  assertLocalDate(date);
  const [year, month, day] = date.split('-').map(Number);
  const value = new Date(Date.UTC(year, month - 1, day + days));
  return `${value.getUTCFullYear()}-${String(value.getUTCMonth() + 1).padStart(2, '0')}-${String(value.getUTCDate()).padStart(2, '0')}`;
}

export function startOfWeek(date, weekStartsOn = 1) {
  assertLocalDate(date);
  const [year, month, day] = date.split('-').map(Number);
  const value = new Date(Date.UTC(year, month - 1, day));
  const offset = (value.getUTCDay() - weekStartsOn + 7) % 7;
  return addLocalDays(date, -offset);
}

export function endOfWeek(date, weekStartsOn = 1) {
  return addLocalDays(startOfWeek(date, weekStartsOn), 6);
}

export function startOfMonth(date) {
  assertLocalDate(date);
  return `${date.slice(0, 7)}-01`;
}

export function endOfMonth(date) {
  assertLocalDate(date);
  const [year, month] = date.split('-').map(Number);
  return new Date(Date.UTC(year, month, 0)).toISOString().slice(0, 10);
}

export function eachLocalDate(startDate, endDate) {
  assertLocalDate(startDate, 'startDate');
  assertLocalDate(endDate, 'endDate');
  const result = [];
  for (let current = startDate; current <= endDate; current = addLocalDays(current, 1)) {
    result.push(current);
  }
  return result;
}
```

将 `task-query-service.js` 中的排序函数复用到新方法：

```js
async range(fromDate, toDate, filters = {}) {
  assertLocalDate(fromDate, 'fromDate');
  assertLocalDate(toDate, 'toDate');
  if (fromDate > toDate) throw new ValidationError('fromDate 不能晚于 toDate');
  const tasks = await this.#tasks();
  return stableSort(
    tasks.filter((task) => (
      task.trashedAt === null
      && task.scheduledDate !== null
      && task.scheduledDate >= fromDate
      && task.scheduledDate <= toDate
      && matchesFilters(task, filters)
    )),
    compareForWorkList(toDate),
  );
}
```

`compareForWorkList` 需要在日期不同的任务之间先比较 `scheduledDate`，再比较星标、优先级、开始时间和创建时间。

- [ ] **Step 4: Run tests to verify they pass**

Run: `node --test tests/domain/dates.test.js tests/services/task-query-service.test.js`

Expected: PASS，且原有日期与时区测试保持通过。

- [ ] **Step 5: Commit**

```bash
git add src/domain/dates.js src/services/task-query-service.js tests/domain/dates.test.js tests/services/task-query-service.test.js
git commit -m "feat: add local date ranges and task range query"
```

### Task P2: 提供明天、本周和月历查询接口

**Files:**
- Modify: `src/services/task-query-service.js`
- Modify: `tests/services/task-query-service.test.js`

**Interfaces:**
- Produces: `TaskQueryService.tomorrow(anchorDate) -> Task[]`。
- Produces: `TaskQueryService.week(anchorDate) -> { startDate, endDate, days, tasks, byDate }`。
- Produces: `TaskQueryService.month(anchorDate) -> { month, startDate, endDate, gridStart, gridEnd, weeks, tasks, byDate }`。
- `byDate` 的键为 `YYYY-MM-DD`，值为该日任务数组。

- [ ] **Step 1: Write the failing test**

```js
test('tomorrow、week 和 month 使用本地边界并分组任务', async () => {
  const { query } = createQuery([
    task({ id: 'wed', scheduledDate: '2026-09-16' }),
    task({ id: 'thu', scheduledDate: '2026-09-17' }),
    task({ id: 'next-month', scheduledDate: '2026-10-01' }),
  ]);

  assert.deepEqual((await query.tomorrow('2026-09-16')).map((item) => item.id), ['thu']);
  const week = await query.week('2026-09-17');
  assert.deepEqual(week.days, [
    '2026-09-14', '2026-09-15', '2026-09-16', '2026-09-17',
    '2026-09-18', '2026-09-19', '2026-09-20',
  ]);
  assert.deepEqual(week.byDate['2026-09-16'].map((item) => item.id), ['wed']);

  const month = await query.month('2026-09-17');
  assert.equal(month.gridStart, '2026-08-31');
  assert.equal(month.gridEnd, '2026-10-04');
  assert.deepEqual(month.byDate['2026-10-01'].map((item) => item.id), ['next-month']);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/services/task-query-service.test.js`

Expected: FAIL，`tomorrow`、`week` 和 `month` 未定义。

- [ ] **Step 3: Write minimal implementation**

在 `task-query-service.js` 中增加一个私有分组函数：

```js
function groupTasksByDate(tasks, dates) {
  const byDate = Object.fromEntries(dates.map((date) => [date, []]));
  for (const task of tasks) {
    if (task.scheduledDate !== null && byDate[task.scheduledDate] !== undefined) {
      byDate[task.scheduledDate].push(task);
    }
  }
  return byDate;
}
```

增加公开方法：

```js
async tomorrow(anchorDate) {
  const date = addLocalDays(anchorDate, 1);
  return this.range(date, date);
}

async week(anchorDate) {
  const startDate = startOfWeek(anchorDate);
  const endDate = endOfWeek(anchorDate);
  const days = eachLocalDate(startDate, endDate);
  const tasks = await this.range(startDate, endDate);
  return { startDate, endDate, days, tasks, byDate: groupTasksByDate(tasks, days) };
}

async month(anchorDate) {
  const startDate = startOfMonth(anchorDate);
  const endDate = endOfMonth(anchorDate);
  const gridStart = startOfWeek(startDate);
  const gridEnd = endOfWeek(endDate);
  const allDates = eachLocalDate(gridStart, gridEnd);
  const tasks = await this.range(gridStart, gridEnd);
  return {
    month: anchorDate.slice(0, 7),
    startDate,
    endDate,
    gridStart,
    gridEnd,
    weeks: Array.from({ length: allDates.length / 7 }, (_, index) => allDates.slice(index * 7, index * 7 + 7)),
    tasks,
    byDate: groupTasksByDate(tasks, allDates),
  };
}
```

方法内部继续调用 `range()`，不得在视图层重新实现日期比较。

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test tests/services/task-query-service.test.js`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/services/task-query-service.js tests/services/task-query-service.test.js
git commit -m "feat: add planning range queries"
```

### Task P3: 增加本周视图

**Files:**
- Create: `src/dashboard/views/week-view.js`
- Modify: `src/dashboard/index.html`
- Modify: `src/dashboard/index.js`
- Modify: `src/dashboard/index.css`
- Test: `tests/e2e/planning.spec.js`

**Interfaces:**
- Consumes: `TaskQueryService.week(anchorDate)`。
- Produces: 路由 `week`，页面标题为“本周”，七个日期段使用 `renderTaskList()` 渲染任务。

- [ ] **Step 1: Write the failing test**

```js
import { test, expect, openDashboard } from './fixtures.js';

test('本周视图展示今天所在周的计划任务', async ({ extension }) => {
  const page = await openDashboard(extension);
  await page.getByLabel('记录一个新事项').fill('本周计划事项');
  await page.locator('#quick-add-date').selectOption('today');
  await page.getByRole('button', { name: '添加', exact: true }).click();

  await page.getByRole('button', { name: '本周' }).click();
  await expect(page.locator('#page-title')).toHaveText('本周');
  await expect(page.getByRole('heading', { name: /本周计划/ })).toBeVisible();
  await expect(page.getByRole('button', { name: '本周计划事项' })).toBeVisible();
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx playwright test tests/e2e/planning.spec.js -g "本周视图"`

Expected: FAIL，导航中不存在“本周”。

- [ ] **Step 3: Write minimal implementation**

在 `src/dashboard/index.html` 的今天导航后增加：

```html
<button type="button" data-route="tomorrow">明天</button>
<button type="button" data-route="week">本周</button>
```

在 `src/dashboard/index.js` 导入 `createWeekView`，增加路由元数据和视图：

```js
import { createWeekView } from './views/week-view.js';

const routeMeta = {
  today: ['今日工作', '现在最需要推进的事项'],
  tomorrow: ['明天', '提前看清下一日安排'],
  week: ['本周', '按天查看这一周的计划'],
  month: ['月历', '从整月范围安排和回看工作'],
  // 保留现有 inbox、all、completed、history、settings 条目
};

const views = {
  // 保留现有视图
  tomorrow: createWeekView({ ...viewOptions, singleDate: true }),
  week: createWeekView(viewOptions),
};
```

`src/dashboard/views/week-view.js`：

```js
import { addLocalDays } from '../../domain/dates.js';
import { renderTaskList } from '../task-list.js';

export function createWeekView({ root, query, today, onAction, onEdit, onError, singleDate = false }) {
  return {
    async render() {
      const anchor = today();
      const tomorrow = addLocalDays(anchor, 1);
      const result = singleDate
        ? {
            startDate: tomorrow,
            endDate: tomorrow,
            days: [tomorrow],
            byDate: { [tomorrow]: await query.tomorrow(anchor) },
          }
        : await query.week(anchor);
      root.innerHTML = `<section class="view-section" aria-labelledby="week-heading">
        <div class="section-heading">
          <div><h2 id="week-heading">${singleDate ? '明天计划' : '本周计划'} · ${result.startDate} 至 ${result.endDate}</h2>
          <p>只显示有计划的日期。完成、取消和删除规则与今天页一致。</p></div>
        </div>
        <div class="week-grid"></div>
      </section>`;
      const grid = root.querySelector('.week-grid');
      for (const date of result.days) {
        const section = document.createElement('section');
        section.className = 'week-day';
        section.innerHTML = `<h3>${date}</h3><div class="week-day__tasks"></div>`;
        grid.append(section);
        renderTaskList(section.querySelector('.week-day__tasks'), result.byDate[date] ?? [], {
          today: anchor,
          onAction,
          onEdit,
          onError,
          emptyMessage: '这一天没有计划。',
        });
      }
    },
  };
}
```

在 `src/dashboard/index.css` 增加：

```css
.week-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 22px 28px; }
.week-day { min-width: 0; }
.week-day h3 { margin: 0 0 8px; color: var(--ink); font-size: var(--text-md); }
.week-day__tasks { border-top: 1px solid var(--line); }
```

在窄屏媒体查询中加入 `.week-grid { grid-template-columns: 1fr; }`。

- [ ] **Step 4: Run test to verify it passes**

Run: `npx playwright test tests/e2e/planning.spec.js -g "本周视图"`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/dashboard/views/week-view.js src/dashboard/index.html src/dashboard/index.js src/dashboard/index.css tests/e2e/planning.spec.js
git commit -m "feat: add week planning view"
```

### Task P4: 增加月历视图

**Files:**
- Create: `src/dashboard/views/month-view.js`
- Modify: `src/dashboard/index.html`
- Modify: `src/dashboard/index.js`
- Modify: `src/dashboard/index.css`
- Test: `tests/e2e/planning.spec.js`

**Interfaces:**
- Consumes: `TaskQueryService.month(anchorDate)`。
- Produces: 路由 `month`，最多六周的日期网格，每个日期格可打开任务，提供上月、下月按钮。

- [ ] **Step 1: Write the failing test**

```js
test('月历展示当前月份任务并支持切换月份', async ({ extension }) => {
  const page = await openDashboard(extension);
  await page.getByLabel('记录一个新事项').fill('月历任务');
  await page.locator('#quick-add-date').selectOption('today');
  await page.getByRole('button', { name: '添加', exact: true }).click();

  await page.getByRole('button', { name: '月历' }).click();
  await expect(page.locator('#page-title')).toHaveText('月历');
  await expect(page.getByRole('grid')).toBeVisible();
  await expect(page.getByRole('button', { name: '月历任务' })).toBeVisible();
  const currentMonth = await page.locator('[data-month-label]').textContent();
  await page.getByRole('button', { name: '下个月' }).click();
  await expect(page.locator('[data-month-label]')).not.toHaveText(currentMonth);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx playwright test tests/e2e/planning.spec.js -g "月历"`

Expected: FAIL，月历路由不存在。

- [ ] **Step 3: Write minimal implementation**

在 `src/dashboard/index.js` 导入并注册：

```js
import { createMonthView } from './views/month-view.js';

const views = {
  // 保留现有视图
  month: createMonthView(viewOptions),
};
```

`src/dashboard/views/month-view.js` 中使用 `query.month(anchor)`：

```js
function shiftMonth(date, offset) {
  const [year, month] = date.split('-').map(Number);
  const value = new Date(Date.UTC(year, month - 1 + offset, 1));
  return `${value.getUTCFullYear()}-${String(value.getUTCMonth() + 1).padStart(2, '0')}-01`;
}

export function createMonthView({ root, query, today, onAction, onEdit, onError }) {
  let anchor = today();
  return {
    async render() {
      const calendar = await query.month(anchor);
      const label = new Intl.DateTimeFormat('zh-CN', { year: 'numeric', month: 'long' })
        .format(new Date(`${calendar.startDate}T00:00:00`));
      root.innerHTML = `<section class="view-section" aria-labelledby="month-heading">
        <div class="section-heading">
          <div><h2 id="month-heading">月历</h2><p data-month-label>${label}</p></div>
          <div class="month-nav" role="group" aria-label="月份切换">
            <button type="button" data-month-nav="-1">上个月</button>
            <button type="button" data-month-nav="1">下个月</button>
          </div>
        </div>
        <div class="month-grid" role="grid" aria-label="${label}">
          ${calendar.weeks.map((week) => `<div class="month-week" role="row">${week.map((date) => {
            const tasks = calendar.byDate[date] ?? [];
            const inMonth = date >= calendar.startDate && date <= calendar.endDate;
            return `<section class="month-day${inMonth ? '' : ' month-day--muted'}" role="gridcell" data-date="${date}">
              <h3>${date.slice(5).replace('-', '月')}日</h3>
              <div class="month-day__tasks"></div>
            </section>`;
          }).join('')}</div>`).join('')}
        </div>
      </section>`;
      for (const cell of root.querySelectorAll('[data-date]')) {
        renderTaskList(cell.querySelector('.month-day__tasks'), calendar.byDate[cell.dataset.date] ?? [], {
          today: anchor,
          onAction,
          onEdit,
          onError,
          emptyMessage: '',
        });
      }
      root.querySelectorAll('[data-month-nav]').forEach((button) => {
        button.addEventListener('click', async () => {
          anchor = shiftMonth(anchor, Number(button.dataset.monthNav));
          await this.render();
        });
      });
    },
  };
}
```

在 `src/dashboard/index.css` 增加：

```css
.month-nav { display: flex; gap: 7px; }
.month-grid { border-top: 1px solid var(--line); border-left: 1px solid var(--line); }
.month-week { display: grid; grid-template-columns: repeat(7, minmax(0, 1fr)); }
.month-day { min-width: 0; min-height: 112px; border-right: 1px solid var(--line); border-bottom: 1px solid var(--line); padding: 8px; }
.month-day--muted { background: var(--canvas); color: var(--muted); }
.month-day h3 { margin: 0 0 5px; font-size: var(--text-xs); }
.month-day .task { grid-template-columns: 22px minmax(0, 1fr) auto; min-height: 36px; }
.month-day .task__meta { display: none; }
.month-day .task__title { white-space: normal; }
```

在 `@media (max-width: 680px)` 中加入 `.month-day { min-height: 76px; padding: 5px; }` 和 `.month-grid { min-width: 0; }`。

- [ ] **Step 4: Run test to verify it passes**

Run: `npx playwright test tests/e2e/planning.spec.js -g "月历"`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/dashboard/views/month-view.js src/dashboard/index.html src/dashboard/index.js src/dashboard/index.css tests/e2e/planning.spec.js
git commit -m "feat: add month calendar view"
```

### Task P5: 生成并复制周/月规则模板总结

**Files:**
- Modify: `src/services/statistics-service.js`
- Create: `src/services/summary-service.js`
- Modify: `src/dashboard/views/history-view.js`
- Modify: `src/dashboard/index.js`
- Modify: `src/dashboard/index.css`
- Create: `tests/services/summary-service.test.js`
- Modify: `tests/e2e/planning.spec.js`

**Interfaces:**
- Consumes: `StatisticsService.weekly(weekStart)`、`StatisticsService.monthly(monthAnchor)`。
- Produces: `SummaryService.weekly(anchorDate)` 和 `SummaryService.monthly(anchorDate)`，返回 `{ kind, title, rangeText, stats, completedTasks, text }`。
- Produces: `SummaryService.copyText(text)`，只调用 `navigator.clipboard.writeText()`，不调用读取 API。

- [ ] **Step 1: Write the failing tests**

在 `tests/services/summary-service.test.js` 中：

```js
test('周总结只使用本地统计和已完成任务标题', async () => {
  const service = new SummaryService({
    statistics: { async weekly() { return { completedCount: 2, plannedCount: 3, postponedCount: 1, completionRate: 2 / 3 }; } },
    query: { async completed() { return [{ title: '完成报价' }, { title: '整理材料' }]; } },
  });

  const summary = await service.weekly('2026-09-17');

  assert.equal(summary.rangeText, '2026-09-14 至 2026-09-20');
  assert.match(summary.text, /实际完成 2 项/);
  assert.match(summary.text, /- 完成报价/);
  assert.doesNotMatch(summary.text, /https?:\\/\\//);
});
```

在 `tests/e2e/planning.spec.js` 增加：

```js
test('工作记录可以生成并复制规则模板总结', async ({ extension }) => {
  const page = await openDashboard(extension);
  await page.getByLabel('记录一个新事项').fill('总结用任务');
  await page.locator('#quick-add-date').selectOption('today');
  await page.getByRole('button', { name: '添加', exact: true }).click();
  await page.getByRole('checkbox', { name: '完成任务' }).first().click();
  await page.getByRole('button', { name: '工作记录' }).click();
  await page.getByRole('button', { name: '生成本周总结' }).click();
  await expect(page.getByRole('textbox', { name: '总结文本' })).toContainText('实际完成');
  await page.evaluate(() => {
    window.__copiedSummary = '';
    Object.defineProperty(navigator, 'clipboard', {
      configurable: true,
      value: { writeText: async (text) => { window.__copiedSummary = text; } },
    });
  });
  await page.getByRole('button', { name: '复制总结' }).click();
  await expect.poll(() => page.evaluate(() => window.__copiedSummary)).toContain('实际完成');
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --test tests/services/summary-service.test.js`

Expected: FAIL，`SummaryService` 模块不存在。

- [ ] **Step 3: Write minimal implementation**

在 `StatisticsService` 中增加：

```js
monthly(anchorDate) {
  assertLocalDate(anchorDate, 'anchorDate');
  return this.#range(startOfMonth(anchorDate), endOfMonth(anchorDate));
}
```

并让 `addDays` 改为复用 `addLocalDays`，避免两套日期逻辑。

创建 `src/services/summary-service.js`：

```js
import { addLocalDays, endOfMonth, endOfWeek, startOfMonth, startOfWeek } from '../domain/dates.js';

function rateText(rate) {
  return rate === null ? '暂无计划' : `${Math.round(rate * 100)}%`;
}

function format({ title, rangeText, stats, completedTasks }) {
  const completed = completedTasks.map((task) => `- ${task.title}`).join('\n');
  return `${title}（${rangeText}）
实际完成 ${stats.completedCount} 项，计划任务 ${stats.plannedCount} 项，延期 ${stats.postponedCount} 次，完成率 ${rateText(stats.completionRate)}。

实际完成明细：
${completed.length === 0 ? '- 暂无' : completed}`;
}

export class SummaryService {
  constructor({ statistics, query }) {
    this.statistics = statistics;
    this.query = query;
  }

  async weekly(anchorDate) {
    const startDate = startOfWeek(anchorDate);
    const endDate = endOfWeek(anchorDate);
    const [stats, completedTasks] = await Promise.all([
      this.statistics.weekly(startDate),
      this.query.completed({ completedFrom: startDate, completedTo: endDate }),
    ]);
    return {
      kind: 'week', title: '本周总结', rangeText: `${startDate} 至 ${endDate}`,
      stats, completedTasks, text: format({ title: '本周总结', rangeText: `${startDate} 至 ${endDate}`, stats, completedTasks }),
    };
  }

  async monthly(anchorDate) {
    const startDate = startOfMonth(anchorDate);
    const endDate = endOfMonth(anchorDate);
    const [stats, completedTasks] = await Promise.all([
      this.statistics.monthly(anchorDate),
      this.query.completed({ completedFrom: startDate, completedTo: endDate }),
    ]);
    return {
      kind: 'month', title: '本月总结', rangeText: `${startDate} 至 ${endDate}`,
      stats, completedTasks, text: format({ title: '本月总结', rangeText: `${startDate} 至 ${endDate}`, stats, completedTasks }),
    };
  }

  async copyText(text) {
    await navigator.clipboard.writeText(text);
  }
}
```

在 `src/dashboard/index.js`：

```js
const summaryService = new SummaryService({ statistics, query });
```

将 `summaryService` 注入 `createHistoryView`。在历史视图的“按日/按周”控制旁增加“按周生成总结”按钮、只读 `textarea aria-label="总结文本"` 和“复制总结”按钮。生成按钮调用 `summaryService.weekly(anchor)` 并写入 textarea；复制按钮只调用 `summaryService.copyText(textarea.value)`。

在 `src/dashboard/index.css` 增加：

```css
.summary-panel { margin-top: 18px; }
.summary-panel textarea { width: 100%; min-height: 150px; font: inherit; }
.summary-actions { display: flex; gap: 8px; margin-top: 8px; }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `node --test tests/services/summary-service.test.js tests/services/statistics-service.test.js`

Run: `npx playwright test tests/e2e/planning.spec.js -g "复制规则模板总结"`

Expected: 两条命令均 PASS。

- [ ] **Step 5: Commit**

```bash
git add src/services/statistics-service.js src/services/summary-service.js src/dashboard/views/history-view.js src/dashboard/index.js src/dashboard/index.css tests/services/summary-service.test.js tests/e2e/planning.spec.js
git commit -m "feat: generate local weekly and monthly summaries"
```

### Task P6: 覆盖计划视图的性能、响应式和键盘验收

**Files:**
- Modify: `tests/services/performance-scenarios.test.js`
- Modify: `tests/e2e/planning.spec.js`
- Modify: `src/dashboard/index.css`

**Interfaces:**
- Consumes: P1-P5 的查询、周视图、月历和总结接口。
- Produces: 10,000 条任务的范围查询、390px 月历无溢出、键盘打开任务和状态文本验收。

- [ ] **Step 1: Write the failing tests**

在 `tests/services/performance-scenarios.test.js` 增加：

```js
test('10,000 条任务的本周和月历范围查询保持确定性', async () => {
  const tasks = makeTasks(10_000);
  const repository = new InMemoryTaskRepository(tasks);
  const query = new TaskQueryService(repository);

  const week = await query.week('2026-09-17');
  const month = await query.month('2026-09-17');

  assert.equal(week.days.length, 7);
  assert.ok(month.gridStart <= '2026-09-01');
  assert.ok(month.gridEnd >= '2026-09-30');
  assert.deepEqual(await repository.list(), tasks);
});
```

在 `tests/e2e/planning.spec.js` 增加：

```js
test('月历在 390px 下可键盘打开任务且没有横向溢出', async ({ extension }) => {
  const page = await openDashboard(extension);
  await page.setViewportSize({ width: 390, height: 844 });
  await page.getByLabel('记录一个新事项').fill('键盘月历任务');
  await page.locator('#quick-add-date').selectOption('today');
  await page.getByRole('button', { name: '添加', exact: true }).click();
  await page.getByRole('button', { name: '月历' }).click();
  await page.getByRole('button', { name: '键盘月历任务' }).focus();
  await page.keyboard.press('Enter');
  await expect(page.locator('#task-editor')).toBeVisible();
  await page.keyboard.press('Escape');
  await expect(page.getByRole('button', { name: '键盘月历任务' })).toBeFocused();

  const overflow = await page.evaluate(() => ({
    scrollWidth: document.documentElement.scrollWidth,
    clientWidth: document.documentElement.clientWidth,
  }));
  expect(overflow.scrollWidth).toBeLessThanOrEqual(overflow.clientWidth + 1);
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --test tests/services/performance-scenarios.test.js`

Expected: FAIL，旧测试数据缺少 V1.1 关系字段或范围方法尚未通过边界断言。

- [ ] **Step 3: Write minimal implementation**

补齐 `makeTasks()` 中每条任务的 `parentId: null`、`tagIds: []`、`seriesId: null`、`occurrenceKey: null`。

在 `src/dashboard/index.css` 的月历与周视图规则中增加：

```css
.month-day,
.week-day,
.task__title,
.task__meta { overflow-wrap: anywhere; }
.month-day .task__more > summary { padding-inline: 4px; }
```

在 `@media (max-width: 680px)` 中将月历日期标题缩短为只显示数字，并保留完整日期在 `aria-label`：

```css
.month-day h3 { font-size: 10px; }
.month-day .task { grid-template-columns: 20px minmax(0, 1fr) auto; }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `node --test tests/services/performance-scenarios.test.js`

Run: `npx playwright test tests/e2e/planning.spec.js`

Expected: 两条命令均 PASS。

- [ ] **Step 5: Commit**

```bash
git add tests/services/performance-scenarios.test.js tests/e2e/planning.spec.js src/dashboard/index.css
git commit -m "test: cover planning view performance and accessibility"
```

## Self-Review

### Spec coverage

- 5 信息架构：明天、本周、月历：P2、P3、P4。
- 8.1 按实际完成日期回顾：P5。
- 8.2 周/月统计口径：P1、P2、P5。
- 8.3 规则模板总结和复制：P5。
- 12 键盘、触达焦点、非颜色化状态：P3、P4、P6。
- 13 10,000 条数据与 390px 视口：P6。

### Placeholder scan

- 无 TBD、TODO 或“按需补充”。
- 日期边界、接口返回结构、DOM 语义和命令均已固定。

### Type consistency

- 所有日期统一使用 `YYYY-MM-DD`。
- 范围接口统一返回 `{ startDate, endDate, tasks, byDate }`，月历额外返回 `weeks` 与网格边界。
- 周/月总结统一复用 `StatisticsService` 的统计结果。
