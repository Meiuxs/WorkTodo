# WorkTodo V1.1 Recurrence Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付“重复模板 + 任务实例”模型，确保完成实例后按规则生成下一实例，并通过 `occurrenceKey` 保证同一 occurrence 最多生成一次。

**Architecture:** 重复规则放在 `domain/recurring-template.js`，规则计算复用本地日期工具；模板由独立仓库保存，首次实例和模板在 IndexedDB 同一事务中创建。完成、取消和停用系列由 `RecurringService` 统一处理，Dashboard 只提交动作并刷新。

**Tech Stack:** Manifest V3、原生 HTML/CSS/ES Modules、IndexedDB、Node.js `node:test`、开发期 `@playwright/test`。

**Spec:** `docs/superpowers/specs/2026-09-17-worktodo-v1.1.md`

**Depends on:** `docs/superpowers/plans/2026-09-17-worktodo-v1.1-foundation.md` 和 `docs/superpowers/plans/2026-09-17-worktodo-v1.1-planning.md` 全部任务完成。

## Global Constraints

- 重复任务只使用“模板 + 实例”，不能在一个任务上叠加未来日期。
- `seriesId` 固定引用模板 ID；`occurrenceKey` 固定为 `${templateId}:${YYYY-MM-DD}` 且唯一。
- 默认在当前实例完成后生成下一实例；未完成实例不自动批量补齐历史任务。
- 取消某次实例不自动取消模板；停用模板不影响已有实例。
- 所有日期按本地时区计算，月末和闰年必须采用“当月最后一天”钳制规则。
- 任务与模板主数据写入 IndexedDB；设置和轻量元数据才写入 `chrome.storage.local`。
- 不新增权限、不发起网络请求、不读取网页内容或剪贴板。
- 每个任务先写失败测试，再写最小实现；每个任务结束提交。

## File Structure

~~~text
src/domain/dates.js                            # addLocalMonthsClamped
src/domain/recurring-template.js               # 重复规则领域模型
src/data/recurring-template-repository.js      # 模板持久化
src/services/recurring-service.js              # 模板和实例业务
src/dashboard/task-editor.js                   # 重复规则编辑
src/dashboard/dashboard-controller.js           # 完成/取消/停用路由
src/dashboard/views/settings-view.js            # 重复模板管理入口
src/dashboard/index.js                          # 服务装配和广播
src/dashboard/index.html                        # 重复编辑器结构
src/dashboard/index.css                         # 重复设置布局
tests/domain/recurring-template.test.js
tests/services/recurring-service.test.js
tests/services/repository-contract.test.js
tests/e2e/recurrence.spec.js
tests/e2e/reliability.spec.js
~~~

---

### Task R1: 定义重复规则和下一日期计算

**Files:**
- Modify: `src/domain/dates.js`
- Create: `src/domain/recurring-template.js`
- Create: `tests/domain/recurring-template.test.js`
- Modify: `tests/domain/dates.test.js`

**Interfaces:**
- Produces: `createRecurringTemplate(input, now, id)`。
- Produces: `validateRecurringTemplate(template)`。
- Produces: `nextOccurrenceDate(template, currentDate) -> YYYY-MM-DD`。
- Produces: `createOccurrenceKey(templateId, occurrenceDate) -> string`。
- Recurrence rule 固定为：
  `{ frequency: 'daily' | 'weekdays' | 'weekly' | 'monthly' | 'yearly' | 'custom', interval: number, unit?: 'day' | 'week' | 'month' }`。

- [ ] **Step 1: Write the failing tests**

在 `tests/domain/dates.test.js` 增加：

```js
test('月份偏移在目标月份天数不足时钳制到月末', () => {
  assert.equal(addLocalMonthsClamped('2026-01-31', 1), '2026-02-28');
  assert.equal(addLocalMonthsClamped('2024-01-31', 1), '2024-02-29');
  assert.equal(addLocalMonthsClamped('2026-12-15', 1), '2027-01-15');
});
```

在 `tests/domain/recurring-template.test.js` 中：

```js
test('重复规则校验并计算每天、工作日和月末日期', () => {
  const daily = createRecurringTemplate({
    title: '日报',
    scheduledDate: '2026-09-17',
    recurrence: { frequency: 'daily', interval: 1 },
  }, NOW, 'template-1');
  assert.equal(nextOccurrenceDate(daily, '2026-09-17'), '2026-09-18');

  const weekdays = createRecurringTemplate({
    title: '周报',
    scheduledDate: '2026-09-18',
    recurrence: { frequency: 'weekdays', interval: 1 },
  }, NOW, 'template-2');
  assert.equal(nextOccurrenceDate(weekdays, '2026-09-18'), '2026-09-21');

  const monthly = createRecurringTemplate({
    title: '月度结算',
    scheduledDate: '2026-01-31',
    recurrence: { frequency: 'monthly', interval: 1 },
  }, NOW, 'template-3');
  assert.equal(nextOccurrenceDate(monthly, '2026-01-31'), '2026-02-28');
  assert.equal(createOccurrenceKey('template-3', '2026-02-28'), 'template-3:2026-02-28');
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --test tests/domain/dates.test.js tests/domain/recurring-template.test.js`

Expected: FAIL，重复模板模块和月份钳制函数不存在。

- [ ] **Step 3: Write minimal implementation**

在 `src/domain/dates.js` 增加：

```js
export function addLocalMonthsClamped(date, months) {
  assertLocalDate(date);
  const [year, month, day] = date.split('-').map(Number);
  const target = new Date(Date.UTC(year, month - 1 + months, 1));
  const lastDay = new Date(Date.UTC(target.getUTCFullYear(), target.getUTCMonth() + 1, 0)).getUTCDate();
  const targetDay = Math.min(day, lastDay);
  return `${target.getUTCFullYear()}-${String(target.getUTCMonth() + 1).padStart(2, '0')}-${String(targetDay).padStart(2, '0')}`;
}
```

创建 `src/domain/recurring-template.js`：

```js
import { ValidationError } from './errors.js';
import { addLocalDays, addLocalMonthsClamped, assertLocalDate } from './dates.js';
import { createTask } from './task.js';

const FREQUENCIES = new Set(['daily', 'weekdays', 'weekly', 'monthly', 'yearly', 'custom']);
const UNITS = new Set(['day', 'week', 'month']);

export function validateRecurringTemplate(template) {
  if (template === null || typeof template !== 'object') throw new ValidationError('重复模板必须是对象');
  if (typeof template.id !== 'string' || template.id.length === 0) throw new ValidationError('重复模板 id 不能为空');
  if (typeof template.active !== 'boolean') throw new ValidationError('active 必须是布尔值');
  if (typeof template.title !== 'string' || template.title.trim().length === 0) throw new ValidationError('重复模板标题不能为空');
  assertLocalDate(template.scheduledDate, 'scheduledDate');
  const rule = template.recurrence;
  if (rule === null || typeof rule !== 'object') throw new ValidationError('recurrence 必须是对象');
  if (!FREQUENCIES.has(rule.frequency)) throw new ValidationError('recurrence.frequency 无效');
  if (!Number.isInteger(rule.interval) || rule.interval < 1 || rule.interval > 90) {
    throw new ValidationError('recurrence.interval 必须是 1 到 90 的整数');
  }
  if (rule.frequency === 'weekdays' && rule.interval !== 1) {
    throw new ValidationError('weekdays 规则只支持每个工作日');
  }
  if (rule.frequency === 'custom' && !UNITS.has(rule.unit)) throw new ValidationError('custom 规则必须指定 day、week 或 month');
  return true;
}

export function createRecurringTemplate(input, now, id) {
  const source = input ?? {};
  const normalized = createTask(source, now, id);
  const template = {
    id,
    active: true,
    title: normalized.title,
    description: normalized.description,
    priority: normalized.priority,
    categoryId: normalized.categoryId,
    tagIds: normalized.tagIds,
    startTime: normalized.startTime,
    dueTime: normalized.dueTime,
    scheduledDate: normalized.scheduledDate,
    recurrence: { interval: 1, ...(source.recurrence ?? {}) },
    sourceTaskId: source.sourceTaskId ?? null,
    createdAt: now,
    updatedAt: now,
  };
  validateRecurringTemplate(template);
  return template;
}

export function createOccurrenceKey(templateId, occurrenceDate) {
  assertLocalDate(occurrenceDate, 'occurrenceDate');
  return `${templateId}:${occurrenceDate}`;
}

export function nextOccurrenceDate(template, currentDate) {
  validateRecurringTemplate(template);
  assertLocalDate(currentDate, 'currentDate');
  const rule = template.recurrence;
  if (rule.frequency === 'daily') return addLocalDays(currentDate, rule.interval);
  if (rule.frequency === 'weekdays') {
    let candidate = addLocalDays(currentDate, 1);
    while ([0, 6].includes(new Date(`${candidate}T00:00:00`).getDay())) {
      candidate = addLocalDays(candidate, 1);
    }
    return candidate;
  }
  if (rule.frequency === 'weekly') return addLocalDays(currentDate, 7 * rule.interval);
  if (rule.frequency === 'monthly') return addLocalMonthsClamped(currentDate, rule.interval);
  if (rule.frequency === 'yearly') return addLocalMonthsClamped(currentDate, 12 * rule.interval);
  if (rule.unit === 'day') return addLocalDays(currentDate, rule.interval);
  if (rule.unit === 'week') return addLocalDays(currentDate, 7 * rule.interval);
  return addLocalMonthsClamped(currentDate, rule.interval);
}
```

`createRecurringTemplate` 先调用 `createTask()` 完成标题、描述、优先级、分类、标签、时间和日期校验，再从规范化结果构造模板；不要在模板模块里复制这些字段校验。

- [ ] **Step 4: Run tests to verify they pass**

Run: `node --test tests/domain/dates.test.js tests/domain/recurring-template.test.js`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/domain/dates.js src/domain/recurring-template.js tests/domain/dates.test.js tests/domain/recurring-template.test.js
git commit -m "feat: add recurring rule model"
```

### Task R2: 持久化模板并创建首个实例

**Files:**
- Create: `src/data/recurring-template-repository.js`
- Modify: `tests/helpers/fakes.js`
- Create: `src/services/recurring-service.js`
- Modify: `src/data/task-repository.js`
- Modify: `src/services/backup-service.js`
- Create: `tests/services/recurring-service.test.js`
- Modify: `tests/services/repository-contract.test.js`

**Interfaces:**
- Produces: `RecurringTemplateRepository.createWithTask(template, task, event)`、`get(id)`、`list()`、`update(template)`。
- Produces: `RecurringService.createTemplate(input) -> { template, task }`。
- Produces: `RecurringService.listTemplates()`、`getTemplate(id)`。
- Consumes: `new RecurringService({ taskService, taskRepository, templateRepository, now, generateId, subtaskService })`。
- Backup v2 的 `recurringTemplates` 参与导出、合并、覆盖和 `replaceAll()`。

- [ ] **Step 1: Write the failing tests**

在 `tests/services/recurring-service.test.js` 中：

```js
function makeRecurringService({ taskRepository, templateRepository }) {
  let sequence = 0;
  const now = () => NOW;
  const generateId = () => `id-${++sequence}`;
  const taskService = new TaskService(taskRepository, { now, generateId });
  return new RecurringService({
    taskService,
    taskRepository,
    templateRepository,
    subtaskService: null,
    now,
    generateId,
  });
}

test('创建重复模板时同时保存首个实例和模板', async () => {
  const taskRepository = new InMemoryTaskRepository();
  const templateRepository = new InMemoryRecurringTemplateRepository();
  const service = makeRecurringService({ taskRepository, templateRepository });

  const result = await service.createTemplate({
    title: '每周提交周报',
    scheduledDate: '2026-09-17',
    recurrence: { frequency: 'weekly', interval: 1 },
  });

  assert.equal(result.template.active, true);
  assert.equal(result.task.seriesId, result.template.id);
  assert.equal(result.task.occurrenceKey, `${result.template.id}:2026-09-17`);
  assert.equal((await templateRepository.get(result.template.id)).title, '每周提交周报');
});
```

在 `tests/services/repository-contract.test.js` 增加：

```js
test('模板和首个实例在同一个仓库边界内可导出和替换', async () => {
  const repo = new InMemoryTaskRepository();
  const templates = new InMemoryRecurringTemplateRepository();
  const service = makeRecurringService({ taskRepository: repo, templateRepository: templates });
  const created = await service.createTemplate({
    title: '每日站会',
    scheduledDate: '2026-09-17',
    recurrence: { frequency: 'daily', interval: 1 },
  });

  const snapshot = await templates.exportAll();
  assert.equal(snapshot.templates.length, 1);
  assert.equal((await repo.get(created.task.id)).id, created.task.id);
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --test tests/services/recurring-service.test.js tests/services/repository-contract.test.js`

Expected: FAIL，模板仓库和服务不存在。

- [ ] **Step 3: Write minimal implementation**

`src/data/recurring-template-repository.js` 与 `TaskRepository` 使用同一数据库连接，创建事务时同时打开 `tasks`、`events`、`recurringTemplates` 三个 store：

在同文件顶部定义不导出的 `clone`、`requestResult`、`transactionResult` 辅助函数，实现与 `src/data/task-repository.js` 相同；不要从任务仓库导出内部辅助函数。

```js
export class RecurringTemplateRepository {
  #database;

  constructor(database = null) {
    this.#database = database ?? openWorkTodoDatabase();
  }

  async createWithTask(template, task, event) {
    validateRecurringTemplate(template);
    validateTask(task);
    const database = await this.#database;
    const transaction = database.transaction(['recurringTemplates', 'tasks', 'events'], 'readwrite');
    transaction.objectStore('recurringTemplates').add(clone(template));
    transaction.objectStore('tasks').add(clone(task));
    transaction.objectStore('events').add(createTaskEvent(event));
    await transactionResult(transaction);
    return { template: clone(template), task: clone(task) };
  }

  async get(id) {
    const database = await this.#database;
    const transaction = database.transaction('recurringTemplates', 'readonly');
    const template = await requestResult(transaction.objectStore('recurringTemplates').get(id));
    await transactionResult(transaction);
    return template === undefined ? undefined : clone(template);
  }

  async list() {
    const database = await this.#database;
    const transaction = database.transaction('recurringTemplates', 'readonly');
    const templates = await requestResult(transaction.objectStore('recurringTemplates').getAll());
    await transactionResult(transaction);
    return templates.map(clone);
  }

  async update(template) {
    validateRecurringTemplate(template);
    const database = await this.#database;
    const transaction = database.transaction('recurringTemplates', 'readwrite');
    transaction.objectStore('recurringTemplates').put(clone(template));
    await transactionResult(transaction);
    return clone(template);
  }

  async exportAll() {
    return { templates: await this.list() };
  }
}
```

`RecurringService.createTemplate(input)`：

```js
async createTemplate(input) {
  const now = this.#now();
  const template = createRecurringTemplate(input, now, this.#generateId());
  const task = createTask({
    ...input,
    seriesId: template.id,
    occurrenceKey: createOccurrenceKey(template.id, input.scheduledDate),
  }, now, this.#generateId());
  const event = createTaskEvent({
    id: this.#generateId(), taskId: task.id, type: 'CREATE', occurredAt: now, detail: { recurringTemplateId: template.id },
  });
  const saved = await this.#templateRepository.createWithTask(template, task, event);
  return saved;
}
```

在 `tests/helpers/fakes.js` 增加 `InMemoryRecurringTemplateRepository`，并提供 `exportAll()`，返回 `{ templates }`。

扩展 `TaskRepository.exportAll()` 和 `replaceAll()`：快照增加 `recurringTemplates`，事务包含 `recurringTemplates`；`BackupService` 的 `createBackup()`、`previewImport()`、合并和覆盖流程都要携带模板数组。旧 v1 备份升级时模板数组固定为空。

- [ ] **Step 4: Run tests to verify they pass**

Run: `node --test tests/services/recurring-service.test.js tests/services/repository-contract.test.js tests/services/backup-service.test.js`

Expected: PASS，模板、首实例和备份 round-trip 全部通过。

- [ ] **Step 5: Commit**

```bash
git add src/data/recurring-template-repository.js src/services/recurring-service.js src/data/task-repository.js src/services/backup-service.js tests/helpers/fakes.js tests/services/recurring-service.test.js tests/services/repository-contract.test.js
git commit -m "feat: persist recurring templates and first instances"
```

### Task R3: 完成后生成下一实例并保证 occurrenceKey 幂等

**Files:**
- Modify: `src/data/task-repository.js`
- Modify: `tests/helpers/fakes.js`
- Modify: `src/services/recurring-service.js`
- Modify: `tests/services/recurring-service.test.js`
- Modify: `tests/services/repository-contract.test.js`

**Interfaces:**
- Consumes: `TaskRepository.findByOccurrenceKey(key)`、`TaskRepository.createIfOccurrenceAbsent(task, event)`。
- Produces: `RecurringService.generateNext(templateId, currentDate) -> Task | null`。
- Produces: `RecurringService.complete(taskId, revision, options) -> { task, nextTask, event }`。
- 生成任务继续携带 `seriesId` 和同一模板的 `occurrenceKey`。

- [ ] **Step 1: Write the failing tests**

```js
function makeRecurringFixture() {
  const taskRepository = new InMemoryTaskRepository();
  const templateRepository = new InMemoryRecurringTemplateRepository();
  return {
    taskRepository,
    templateRepository,
    service: makeRecurringService({ taskRepository, templateRepository }),
  };
}

test('完成后生成下一实例且同一 occurrenceKey 不重复', async () => {
  const { service, taskRepository, templateRepository } = makeRecurringFixture();
  const created = await service.createTemplate({
    title: '每日复盘',
    scheduledDate: '2026-09-17',
    recurrence: { frequency: 'daily', interval: 1 },
  });

  const completed = await service.complete(created.task.id, created.task.revision);
  const duplicate = await service.generateNext(created.template.id, '2026-09-17');
  const all = await taskRepository.list();

  assert.equal(completed.task.lifecycle, 'completed');
  assert.equal(completed.nextTask.scheduledDate, '2026-09-18');
  assert.equal(duplicate.id, completed.nextTask.id);
  assert.equal(all.filter((task) => task.seriesId === created.template.id).length, 2);
  assert.equal((await templateRepository.get(created.template.id)).active, true);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/services/recurring-service.test.js`

Expected: FAIL，`generateNext()` 和 `createIfOccurrenceAbsent()` 未定义。

- [ ] **Step 3: Write minimal implementation**

在 `TaskRepository` 增加：

```js
async findByOccurrenceKey(occurrenceKey) {
  const database = await this.#getDatabase();
  const transaction = database.transaction('tasks', 'readonly');
  const task = await requestResult(transaction.objectStore('tasks').index('occurrenceKey').get(occurrenceKey));
  await transactionResult(transaction);
  return task === undefined ? undefined : clone(task);
}

async createIfOccurrenceAbsent(task, event) {
  const existing = await this.findByOccurrenceKey(task.occurrenceKey);
  if (existing !== undefined) return { created: false, task: existing };
  try {
    const saved = await this.create(task, event);
    return { created: true, task: saved };
  } catch (error) {
    if (error?.name !== 'ConstraintError') throw error;
    const fallback = await this.findByOccurrenceKey(task.occurrenceKey);
    return { created: false, task: fallback };
  }
}
```

内存仓库实现相同结果，并模拟唯一索引冲突。

在 `RecurringService` 增加：

```js
async #completeTask(taskId, revision, options) {
  return this.#subtaskService === null
    ? this.#taskService.complete(taskId, revision)
    : this.#subtaskService.completeParent(taskId, revision, options);
}

async generateNext(templateId, currentDate) {
  const template = await this.#templateRepository.get(templateId);
  if (template === undefined || !template.active) return null;
  const nextDate = nextOccurrenceDate(template, currentDate);
  const occurrenceKey = createOccurrenceKey(template.id, nextDate);
  const existing = await this.#taskRepository.findByOccurrenceKey(occurrenceKey);
  if (existing !== undefined) return existing;
  const now = this.#now();
  const nextTask = createTask({
    title: template.title,
    description: template.description,
    priority: template.priority,
    categoryId: template.categoryId,
    tagIds: template.tagIds,
    startTime: template.startTime,
    dueTime: template.dueTime,
    scheduledDate: nextDate,
    seriesId: template.id,
    occurrenceKey,
  }, now, this.#generateId());
  const event = createTaskEvent({
    id: this.#generateId(), taskId: nextTask.id, type: 'CREATE', occurredAt: now,
    detail: { recurringTemplateId: template.id, previousOccurrence: currentDate },
  });
  const result = await this.#taskRepository.createIfOccurrenceAbsent(nextTask, event);
  return result.task;
}

async complete(taskId, revision, options = {}) {
  const current = await this.#taskService.getTask(taskId);
  const result = await this.#completeTask(taskId, revision, options);
  const nextTask = current.seriesId === null
    ? null
    : await this.generateNext(current.seriesId, current.scheduledDate);
  return { ...result, nextTask };
}
```

`#completeTask` 在任务有未完成子任务时调用 `SubtaskService.completeParent`，否则调用 `TaskService.complete`。这样 O4 的父任务门禁不会被重复任务的下一实例生成绕过。

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test tests/services/recurring-service.test.js tests/services/repository-contract.test.js`

Expected: PASS，首次生成一条下一实例，重复调用只返回已有实例。

- [ ] **Step 5: Commit**

```bash
git add src/data/task-repository.js tests/helpers/fakes.js src/services/recurring-service.js tests/services/recurring-service.test.js tests/services/repository-contract.test.js
git commit -m "feat: generate idempotent recurring instances"
```

### Task R4: 在任务编辑器中配置重复规则

**Files:**
- Modify: `src/dashboard/index.html`
- Modify: `src/dashboard/task-editor.js`
- Modify: `src/dashboard/index.js`
- Modify: `src/dashboard/dashboard-controller.js`
- Modify: `tests/services/dashboard-controller.test.js`
- Modify: `tests/e2e/recurrence.spec.js`

**Interfaces:**
- Consumes: `RecurringService.createTemplate(input)`。
- Produces: `createTaskEditor()` 新增任务的 `recurrence` 字段，值为 `null` 或规则对象。
- Produces: `DashboardController.createTask(input)`：有 `recurrence` 时调用 `RecurringService`，否则保持原 `TaskService.create()` 行为。

- [ ] **Step 1: Write the failing test**

```js
import { test, expect, openDashboard } from './fixtures.js';

test('编辑器可以创建每天重复任务并在完成后生成下一项', async ({ extension }) => {
  const page = await openDashboard(extension);
  await page.getByLabel('记录一个新事项').fill('每日日报');
  await page.locator('#quick-add-date').selectOption('today');
  await page.getByRole('button', { name: '更多字段' }).click();
  const editor = page.locator('#task-editor');
  await editor.getByLabel('重复').selectOption('daily');
  await editor.getByLabel('重复间隔').fill('1');
  await editor.getByRole('button', { name: '保存任务' }).click();
  await page.getByRole('checkbox', { name: '完成任务' }).first().click();

  await page.getByRole('button', { name: '明天' }).click();
  await expect(page.getByRole('button', { name: '每日日报' })).toBeVisible();
});
```

在 `tests/services/dashboard-controller.test.js` 增加：

```js
test('带重复规则的新任务交给 RecurringService', async () => {
  const calls = [];
  const view = { async render() {} };
  const controller = new DashboardController({
    views: { today: view },
    taskService: {},
    recurringService: {
      async createTemplate(input) {
        calls.push(input);
        return { template: { id: 'template-1' }, task: { id: 'task-1', revision: 0 } };
      },
    },
  });

  await controller.createTask({ title: '日报', recurrence: { frequency: 'daily', interval: 1 } });

  assert.equal(calls.length, 1);
  assert.equal(calls[0].title, '日报');
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/services/dashboard-controller.test.js`

Expected: FAIL，`createTask()` 不识别 `recurringService`。

- [ ] **Step 3: Write minimal implementation**

在 `index.html` 的任务编辑器增加重复字段：

```html
<fieldset class="recurrence-picker" data-recurrence-picker>
  <legend>重复</legend>
  <label>重复<select name="recurrenceFrequency">
    <option value="none">不重复</option><option value="daily">每天</option><option value="weekdays">工作日</option><option value="weekly">每周</option><option value="monthly">每月</option><option value="yearly">每年</option><option value="custom">自定义</option>
  </select></label>
  <label>重复间隔<input name="recurrenceInterval" type="number" min="1" max="90" value="1"></label>
  <label data-custom-unit>自定义单位<select name="recurrenceUnit"><option value="day">天</option><option value="week">周</option><option value="month">月</option></select></label>
</fieldset>
```

`readChanges()` 增加：

```js
const frequency = field(form, 'recurrenceFrequency').value;
recurrence: frequency === 'none'
  ? null
  : {
      frequency,
      interval: Number(field(form, 'recurrenceInterval').value),
      ...(frequency === 'custom' ? { unit: field(form, 'recurrenceUnit').value } : {}),
    },
```

`DashboardController.createTask(input)`：

```js
async createTask(input) {
  const result = input?.recurrence && this.#recurringService !== null
    ? await this.#recurringService.createTemplate(input)
    : await this.#taskService.create(input);
  await this.#sendMessage({ type: 'TASK_CHANGED', taskId: result.task.id });
  await this.refresh();
  return result;
}
```

在 `src/dashboard/index.js` 创建 `recurringService`，并传给 `DashboardController`。编辑已有实例时只显示重复规则摘要和“停用后续重复”按钮，不允许在一个实例上直接修改未来规则。

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test tests/services/dashboard-controller.test.js`

Run: `npx playwright test tests/e2e/recurrence.spec.js -g "每天重复"`

Expected: 两条命令均 PASS。

- [ ] **Step 5: Commit**

```bash
git add src/dashboard/index.html src/dashboard/task-editor.js src/dashboard/index.js src/dashboard/dashboard-controller.js tests/services/dashboard-controller.test.js tests/e2e/recurrence.spec.js
git commit -m "feat: configure recurring tasks in editor"
```

### Task R5: 处理取消单次实例、停用模板和系列行为

**Files:**
- Modify: `src/services/recurring-service.js`
- Modify: `src/dashboard/dashboard-controller.js`
- Modify: `src/dashboard/task-editor.js`
- Modify: `src/dashboard/index.html`
- Modify: `src/dashboard/index.js`
- Modify: `src/dashboard/views/settings-view.js`
- Modify: `tests/services/recurring-service.test.js`
- Modify: `tests/e2e/recurrence.spec.js`

**Interfaces:**
- Produces: `RecurringService.cancelOccurrence(taskId, revision) -> { task, nextTask, event }`。
- Produces: `RecurringService.deactivate(templateId) -> { template }`。
- 既有实例不因 `deactivate()` 改变生命周期、日期或历史字段。

- [ ] **Step 1: Write the failing tests**

```js
test('取消单个实例继续系列，但停用模板阻止后续生成', async () => {
  const { service, taskRepository } = makeRecurringFixture();
  const created = await service.createTemplate({
    title: '每周例会',
    scheduledDate: '2026-09-17',
    recurrence: { frequency: 'weekly', interval: 1 },
  });

  const cancelled = await service.cancelOccurrence(created.task.id, created.task.revision);
  assert.equal(cancelled.task.lifecycle, 'cancelled');
  assert.equal(cancelled.nextTask.scheduledDate, '2026-09-24');
  assert.equal((await taskRepository.exportAll()).recurringTemplates[0].active, true);

  const template = await service.getTemplate(created.template.id);
  await service.deactivate(template.id);
  const stopped = await service.generateNext(template.id, '2026-09-24');
  assert.equal(stopped, null);
  assert.equal((await taskRepository.get(cancelled.nextTask.id)).lifecycle, 'todo');
});
```

在 `tests/e2e/recurrence.spec.js` 增加：

```js
test('编辑器停用后续重复不会删除既有实例', async ({ extension }) => {
  const page = await openDashboard(extension);
  await page.getByLabel('记录一个新事项').fill('系列会议');
  await page.locator('#quick-add-date').selectOption('today');
  await page.getByRole('button', { name: '更多字段' }).click();
  const editor = page.locator('#task-editor');
  await editor.getByLabel('重复').selectOption('weekly');
  await editor.getByRole('button', { name: '保存任务' }).click();
  await page.getByRole('button', { name: '系列会议' }).click();
  await editor.getByRole('button', { name: '停用后续重复' }).click();
  await expect(editor.getByText('已停用', { exact: true })).toBeVisible();
  await editor.getByRole('button', { name: '取消' }).click();
  await expect(page.getByRole('button', { name: '系列会议' })).toBeVisible();
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --test tests/services/recurring-service.test.js`

Expected: FAIL，`cancelOccurrence()` 和 `deactivate()` 未定义。

- [ ] **Step 3: Write minimal implementation**

在 `RecurringService` 增加：

```js
async cancelOccurrence(taskId, revision) {
  const current = await this.#taskService.getTask(taskId);
  const result = await this.#taskService.cancel(taskId, revision);
  const nextTask = current.seriesId === null
    ? null
    : await this.generateNext(current.seriesId, current.scheduledDate);
  return { ...result, nextTask };
}

async deactivate(templateId) {
  const template = await this.#templateRepository.get(templateId);
  if (template === undefined) throw new ValidationError(`重复模板 ${templateId} 不存在`);
  const next = { ...template, active: false, updatedAt: this.#now() };
  return { template: await this.#templateRepository.update(next) };
}
```

`DashboardController.handleTaskAction` 在存在 `recurringService` 时：

- `complete` 调用 `recurringService.complete(taskId, revision, { force: value?.force === true })`；
- `cancel` 调用 `recurringService.cancelOccurrence(taskId, revision)`；
- 其他动作保持原路径。

在任务编辑器中，现有实例的重复摘要区域增加“停用后续重复”按钮，调用注入的 `onDeactivateSeries(templateId)`；停用成功后重新加载任务并把摘要改为“已停用”，不得修改当前实例的 `lifecycle`。

```js
onDeactivateSeries: async (templateId) => {
  const result = await recurringService.deactivate(templateId);
  showToast('已停用后续重复');
  return result;
},
```

在设置页增加“重复模板”只读列表，显示标题、规则摘要、`active` 的“运行中/已停用”文字，动作仅提供“停用”。列表只读取模板仓库，不直接修改任务。

- [ ] **Step 4: Run tests to verify they pass**

Run: `node --test tests/services/recurring-service.test.js`

Run: `npx playwright test tests/e2e/recurrence.spec.js -g "停用后续重复"`

Expected: 两条命令均 PASS。

- [ ] **Step 5: Commit**

```bash
git add src/services/recurring-service.js src/dashboard/dashboard-controller.js src/dashboard/task-editor.js src/dashboard/index.html src/dashboard/index.js src/dashboard/views/settings-view.js tests/services/recurring-service.test.js tests/e2e/recurrence.spec.js
git commit -m "feat: manage recurring series lifecycle"
```

### Task R6: 覆盖重复任务重启恢复、幂等和窄屏回归

**Files:**
- Modify: `tests/e2e/recurrence.spec.js`
- Modify: `tests/e2e/reliability.spec.js`
- Modify: `tests/services/recurring-service.test.js`
- Modify: `src/dashboard/index.css`

**Interfaces:**
- Consumes: R1-R5 的模板、实例、完成、取消和停用接口。
- Produces: 浏览器刷新后模板和下一实例仍在；重复 completion 不生成重复 occurrence；390px 编辑器无溢出。

- [ ] **Step 1: Write the failing tests**

在 `tests/e2e/recurrence.spec.js` 增加：

```js
test('重复任务在 Dashboard 刷新后仍保持模板和下一实例', async ({ extension }) => {
  const page = await openDashboard(extension);
  await page.getByLabel('记录一个新事项').fill('每日回顾');
  await page.locator('#quick-add-date').selectOption('today');
  await page.getByRole('button', { name: '更多字段' }).click();
  const editor = page.locator('#task-editor');
  await editor.getByLabel('重复').selectOption('daily');
  await editor.getByRole('button', { name: '保存任务' }).click();
  await page.getByRole('checkbox', { name: '完成任务' }).first().click();
  await page.reload();
  await page.getByRole('button', { name: '明天' }).click();
  await expect(page.getByRole('button', { name: '每日回顾' })).toBeVisible();
});
```

在 `tests/services/recurring-service.test.js` 增加：

```js
test('同一模板和日期重复生成只返回已有实例', async () => {
  const { service } = makeRecurringFixture();
  const created = await service.createTemplate({
    title: '幂等验证',
    scheduledDate: '2026-09-17',
    recurrence: { frequency: 'daily', interval: 1 },
  });
  const first = await service.generateNext(created.template.id, '2026-09-17');
  const second = await service.generateNext(created.template.id, '2026-09-17');

  assert.equal(first.id, second.id);
});
```

在 `tests/e2e/reliability.spec.js` 增加：

```js
test('390px 下重复规则编辑器不产生横向溢出', async ({ extension }) => {
  const page = await openDashboard(extension);
  await page.setViewportSize({ width: 390, height: 844 });
  await page.getByLabel('记录一个新事项').fill('窄屏重复任务');
  await page.locator('#quick-add-date').selectOption('today');
  await page.getByRole('button', { name: '更多字段' }).click();
  const editor = page.locator('#task-editor');
  await editor.getByLabel('重复').selectOption('custom');
  const overflow = await page.evaluate(() => ({
    scrollWidth: document.documentElement.scrollWidth,
    clientWidth: document.documentElement.clientWidth,
  }));
  expect(overflow.scrollWidth).toBeLessThanOrEqual(overflow.clientWidth + 1);
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --test tests/services/recurring-service.test.js`

Expected: 如果 R3 已正确实现唯一 occurrenceKey，则单元测试通过；新增 E2E 在 `npx playwright test tests/e2e/recurrence.spec.js tests/e2e/reliability.spec.js -g "重复"` 时失败，因为旧窄屏布局无法容纳自定义单位控件。

- [ ] **Step 3: Write minimal implementation**

在 `src/dashboard/index.css` 增加：

```css
.recurrence-picker { display: grid; gap: 10px; margin-top: 16px; border-top: 1px solid var(--line); border-bottom: 1px solid var(--line); padding: 14px 0; }
.recurrence-picker label { display: grid; gap: 5px; color: var(--muted); font-size: var(--text-xs); }
@media (max-width: 680px) {
  .recurrence-picker { grid-template-columns: 1fr; }
  .recurrence-picker select,
  .recurrence-picker input { width: 100%; }
}
```

确认 `RecurringService.complete()` 在任务已是完成状态时不会再次生成下一实例；重复调用应在 revision 冲突处终止，唯一 `occurrenceKey` 负责最终幂等保护。扩展 `TaskRepository.createIfOccurrenceAbsent()` 的错误处理，只把唯一索引冲突视为已存在，不吞掉其他事务错误。

- [ ] **Step 4: Run tests to verify they pass**

Run: `node --test tests/services/recurring-service.test.js`

Run: `npx playwright test tests/e2e/recurrence.spec.js tests/e2e/reliability.spec.js -g "重复"`

Expected: 两条命令均 PASS，刷新后任务和模板仍存在，窄屏无横向溢出。

- [ ] **Step 5: Commit**

```bash
git add tests/e2e/recurrence.spec.js tests/e2e/reliability.spec.js tests/services/recurring-service.test.js src/dashboard/index.css
git commit -m "test: cover recurring task recovery and idempotency"
```

## Self-Review

### Spec coverage

- 6.7 模板 + 实例、每天、工作日、每周、每月、每年和自定义：R1、R2、R4。
- 6.7 `seriesId` 和唯一 `occurrenceKey`：R1、R2、R3。
- 6.7 完成后生成下一实例、取消单次和停用模板：R3、R5。
- 9.1 `recurringTemplates` store：R2。
- 9.3 模板备份兼容：R2。
- 12 键盘和窄屏：R6。

### Placeholder scan

- 无 TBD、TODO 或“按需补充”。
- 规则频率、间隔边界、模板字段、实例幂等键和系列动作均已固定。

### Type consistency

- 模板统一使用 `id`、`active`、`recurrence`、`sourceTaskId` 字段。
- 实例统一使用 `seriesId: template.id` 和 `occurrenceKey: templateId:YYYY-MM-DD`。
- 完成、取消和停用统一经过 `RecurringService`。
